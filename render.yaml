services:
  - type: web
    name: n8n-elvis-new
    env: docker
    region: oregon
    plan: starter
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: "5678"
      - key: N8N_PORT
        value: "5678"
    healthCheckPath: /healthz
